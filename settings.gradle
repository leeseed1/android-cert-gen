pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        // 兼容 Groovy 的写法，使用闭包和 url 属性
        maven {
            url 'https://jetbrains.space'
        }
    }
}

rootProject.name = "CertGenerator"
