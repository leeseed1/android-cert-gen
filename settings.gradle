pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        // 修正点：将 maven("...") 改为标准的 MavenArtifactRepository 写法
        maven { url = java.net.URI("https://jetbrains.space") }
    }
}
rootProject.name = "CertGenerator"
