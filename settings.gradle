pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        // 兼容 Groovy 和 Kotlin 的双通用写法，直接用字符串传参
        maven { url = "https://jetbrains.space" }
    }
}
rootProject.name = "CertGenerator"
