近期的某ray要求对证书SHA256进行核对，很多某ray用户嫌麻烦喜欢用10年的自签证书。但在使用自签证书时发现，PX端都可以用自签证书，但是andxxxxx端却不行。

这并不是你的那个东西有问题，而是自签证书不是通过ca机构发签发的，无权接入。如果andxxxx导入自签证书会提示“需要私钥”，也是这个原因。

本工具就是为了解决此问题，它需要GIT，按提示即可生成crt和key，包括sha256码。

将生成的crt和key用于那个地方，再将crt上传的andxxxx端导入信任证书，你会在用户证书里看到此证书。重试那个东西就通了。
