近期的xray要求对证书SHA256进行核对，很多xray用户在一些节点用自签证书时发现，pc端和openwrt端都可以用自签证书，但是安卓端却不行。

这并不是你的节点有问题，而是自签证书不是通过ca机构发签发的，只能用于调试，安卓系统如果导入自签证书会提示“需要私钥”，也是这个原因。

本工具就是为了解决此问题，它需要GIT，按提示即可生成crt和key，包括sha256码。

将生成的crt和key用于节点的，再将crt上传的手机关导入信任证书，你会在用户证书里看到此证书。再导出节点配对即可。
